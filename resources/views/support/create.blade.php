<!DOCTYPE html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Nueva solicitud - Plaza Local</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="min-h-screen bg-brand-surface pb-24 text-brand-ink antialiased"><x-market-nav :back-url="route('support.index')" />
<main class="mx-auto max-w-3xl px-5 py-9"><p class="text-xs font-black uppercase tracking-[.18em] text-brand-orange">Soporte Plaza Local</p><h1 class="mt-2 text-3xl font-black">¿Cómo podemos ayudarte?</h1><p class="mt-2 text-sm font-semibold text-brand-muted">Tu solicitud quedará registrada y solo será visible para ti y administración.</p>
<form class="mt-7 space-y-5 rounded-[1.75rem] border border-brand/10 bg-white p-6 shadow-sm" method="POST" action="{{ route('support.store') }}">@csrf
<label class="block"><span class="text-sm font-black">Tipo de ayuda</span><select class="mt-2 w-full rounded-2xl border border-brand/10 bg-brand-surface px-4 py-3" name="category" required>@foreach($categories as $value=>$label)<option value="{{ $value }}" @selected(old('category',$category)===$value)>{{ $label }}</option>@endforeach</select>@error('category')<span class="text-sm font-bold text-red-600">{{ $message }}</span>@enderror</label>
<label class="block"><span class="text-sm font-black">Asunto</span><input class="mt-2 w-full rounded-2xl border border-brand/10 bg-brand-surface px-4 py-3" name="subject" value="{{ old('subject',$subject) }}" minlength="5" maxlength="160" required>@error('subject')<span class="text-sm font-bold text-red-600">{{ $message }}</span>@enderror</label>
<label class="block"><span class="text-sm font-black">Explícanos qué ocurrió</span><textarea class="mt-2 min-h-40 w-full rounded-2xl border border-brand/10 bg-brand-surface px-4 py-3" name="body" minlength="10" maxlength="5000" required placeholder="Incluye fechas, qué intentabas hacer y qué solución necesitas.">{{ old('body') }}</textarea>@error('body')<span class="text-sm font-bold text-red-600">{{ $message }}</span>@enderror</label>
<button class="w-full rounded-full bg-brand px-6 py-3.5 font-black text-white" type="submit">Enviar a soporte</button></form></main><x-bottom-nav active="more" /></body></html>
