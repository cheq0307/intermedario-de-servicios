<x-admin-layout title="Disputas" section="disputes"><main class="mx-auto max-w-5xl px-5 py-9"><p class="text-xs font-black uppercase tracking-[.18em] text-brand-orange">Administración</p><h1 class="mt-2 text-3xl font-black">Disputas</h1><div class="mt-7 space-y-4">@forelse($disputes as $dispute)<a class="flex flex-wrap items-center justify-between gap-4 rounded-[1.5rem] border border-brand/10 bg-white p-5 shadow-sm" href="{{ route('disputes.show', $dispute) }}"><div><strong class="text-lg">{{ $dispute->order->jobRequest?->title ?? 'Orden '.$dispute->order_id }}</strong><p class="mt-1 text-sm font-bold text-brand-muted">Abierta por {{ $dispute->opener->name }}</p></div><span class="rounded-full px-3 py-2 text-xs font-black {{ $dispute->status->value === 'open' ? 'bg-brand-orange-soft text-brand-danger-warm' : 'bg-brand-success-soft text-brand-success' }}">{{ $dispute->status->value === 'open' ? 'Pendiente' : 'Resuelta' }}</span></a>@empty<div class="rounded-3xl border border-dashed p-10 text-center font-bold text-brand-muted">No hay disputas.</div>@endforelse</div><div class="mt-6">{{ $disputes->links() }}</div></main></x-admin-layout>
