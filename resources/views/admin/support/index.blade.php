<x-admin-layout title="Soporte" section="support">
<main class="mx-auto max-w-6xl px-5 py-9"><p class="text-xs font-black uppercase tracking-[.18em] text-brand-orange">Atención al usuario</p><h1 class="mt-2 text-3xl font-black">Casos de soporte</h1><p class="mt-2 text-sm font-semibold text-brand-muted">Responde solicitudes, documenta revisiones y controla sus estados.</p>
<form class="mt-6 grid gap-3 rounded-3xl bg-white p-5 shadow-sm sm:grid-cols-[1fr_240px_auto]" method="GET"><input class="rounded-2xl bg-brand-surface px-4 py-3" name="q" value="{{ $filters['q'] ?? '' }}" placeholder="Asunto, usuario o correo"><select class="rounded-2xl bg-brand-surface px-4 py-3" name="status"><option value="">Todos los estados</option>@foreach($statuses as $value=>$label)<option value="{{ $value }}" @selected(($filters['status']??'')===$value)>{{ $label }}</option>@endforeach</select><button class="rounded-full bg-brand px-5 py-3 font-black text-white">Filtrar</button></form>
<div class="mt-6 grid gap-3">@forelse($tickets as $ticket)<a class="rounded-3xl border border-brand/10 bg-white p-5 shadow-sm" href="{{ route('admin.support.show',$ticket) }}"><div class="flex flex-wrap items-start justify-between gap-3"><div><span class="text-xs font-black uppercase tracking-wide text-brand-danger-warm">{{ $ticket->reference }} · {{ $ticket->category_label }}</span><h2 class="mt-2 text-lg font-black">{{ $ticket->subject }}</h2><p class="mt-1 text-sm font-semibold text-brand-muted">{{ $ticket->user->name }} · {{ $ticket->user->email }}</p></div><span class="rounded-full bg-brand-success-soft px-3 py-1 text-xs font-black text-brand-success">{{ $ticket->status_label }}</span></div><p class="mt-3 text-xs font-bold text-brand-caption">{{ $ticket->messages_count }} mensajes · {{ optional($ticket->last_message_at)->diffForHumans() }}</p></a>@empty<div class="rounded-3xl border border-dashed bg-white p-12 text-center font-bold text-brand-muted">No hay casos con estos filtros.</div>@endforelse</div>{{ $tickets->links() }}</main></x-admin-layout>
