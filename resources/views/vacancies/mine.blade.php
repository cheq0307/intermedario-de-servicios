<!DOCTYPE html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Mi empleo - Plaza Local</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="min-h-screen bg-brand-surface pb-24 text-brand-ink"><x-market-nav :back-url="route('vacancies.index')" /><main class="mx-auto max-w-5xl px-4 py-7"><div class="flex items-end justify-between gap-3"><h1 class="text-3xl font-black">Mis vacantes y postulaciones</h1><a class="rounded-full bg-brand px-5 py-3 font-black text-white" href="{{ route('vacancies.create') }}">Publicar vacante</a></div><div class="mt-7 grid gap-6 lg:grid-cols-2"><section><h2 class="text-xl font-black">Vacantes publicadas</h2>@forelse($owned as $vacancy)<a class="mt-3 block rounded-2xl border bg-white p-5" href="{{ route('vacancies.show',$vacancy) }}"><strong>{{ $vacancy->title }}</strong><p>{{ $vacancy->status }} · {{ $vacancy->applications_count }} postulaciones</p></a>@empty<p class="mt-3 rounded-2xl border border-dashed p-6">No has publicado vacantes.</p>@endforelse</section><section><h2 class="text-xl font-black">Mis postulaciones</h2>@forelse($applications as $application)<a class="mt-3 block rounded-2xl border bg-white p-5" href="{{ route('vacancies.show',$application->vacancy) }}"><strong>{{ $application->vacancy->title }}</strong><p>{{ $application->vacancy->community->name }} · {{ $application->status }}</p></a>@empty<p class="mt-3 rounded-2xl border border-dashed p-6">No te has postulado.</p>@endforelse</section></div></main><x-bottom-nav active="orders" /></body></html>