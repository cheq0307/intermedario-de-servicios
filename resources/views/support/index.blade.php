<!DOCTYPE html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width, initial-scale=1"><title>Soporte - Plaza Local</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="min-h-screen bg-brand-page pb-24 text-brand-ink antialiased"><x-market-nav :back-url="route('more.index')" />
<main class="mx-auto max-w-5xl px-5 py-9"><div class="flex flex-wrap items-end justify-between gap-4"><div><p class="text-xs font-black uppercase tracking-[.18em] text-brand-orange">Centro de ayuda</p><h1 class="mt-2 text-3xl font-black">Mis solicitudes</h1><p class="mt-2 text-sm font-semibold text-brand-muted">Consulta respuestas y el estado de cada caso.</p></div><a class="rounded-full bg-brand-orange px-5 py-3 font-black text-white" href="{{ route('support.create') }}">Nueva solicitud</a></div>
@if(session('status'))<p class="mt-5 rounded-2xl bg-brand-success-soft p-4 text-sm font-black text-brand-success">{{ session('status') }}</p>@endif
<div class="mt-7 grid gap-3">@forelse($tickets as $ticket)<a class="rounded-3xl border border-brand/10 bg-white p-5 shadow-sm transition hover:-translate-y-0.5" href="{{ route('support.show',$ticket) }}"><div class="flex flex-wrap items-start justify-between gap-3"><div><span class="text-xs font-black uppercase tracking-wide text-brand-danger-warm">{{ $ticket->reference }} · {{ $ticket->category_label }}</span><h2 class="mt-2 text-lg font-black">{{ $ticket->subject }}</h2></div><span class="rounded-full bg-brand-success-soft px-3 py-1 text-xs font-black text-brand-success">{{ $ticket->status_label }}</span></div><p class="mt-3 text-xs font-bold text-brand-muted">{{ $ticket->messages_count }} mensajes · Actualizado {{ optional($ticket->last_message_at)->diffForHumans() }}</p></a>@empty<div class="rounded-3xl border border-dashed bg-white p-12 text-center"><h2 class="text-xl font-black">No tienes solicitudes</h2><p class="mt-2 text-sm font-semibold text-brand-muted">Cuando necesites ayuda, podrás seguirla desde aquí.</p></div>@endforelse</div>{{ $tickets->links() }}</main><x-bottom-nav active="more" /></body></html>
