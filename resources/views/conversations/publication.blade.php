<!DOCTYPE html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ $title }} - Plaza Local</title>@vite(['resources/css/app.css','resources/js/app.js'])</head>
<body class="min-h-screen bg-brand-surface pb-24 text-brand-ink"><x-market-nav :back-url="route('conversations.show',$conversation)" />
<main class="mx-auto max-w-3xl p-5"><article class="rounded-2xl bg-white p-5 shadow-sm"><p class="text-xs font-black uppercase text-brand-orange">Publicación vinculada al chat</p><h1 class="mt-2 text-2xl font-black">{{ $title }}</h1><p class="mt-4 whitespace-pre-wrap">{{ $post->body }}</p>@if($post->listing?->description)<p class="mt-4 whitespace-pre-wrap">{{ $post->listing->description }}</p>@endif @if($post->jobRequest?->description)<p class="mt-4 whitespace-pre-wrap">{{ $post->jobRequest->description }}</p>@endif
<div class="mt-4 grid gap-3 sm:grid-cols-2">@foreach($post->media->where('type','image') as $media)<img class="w-full rounded-xl" src="{{ $media->url }}" alt="{{ $media->alt_text ?: $title }}">@endforeach</div>
@if($post->user)<a class="mt-5 inline-flex min-h-11 items-center font-bold text-brand-success underline" href="{{ route('profile.show',$post->user) }}">Ver perfil de {{ $post->user->name }}</a>@endif</article></main><x-bottom-nav active="messages" /></body></html>
